name: ollama
description: Run Ollama large language models in GitHub Actions
author: remarkablemark
inputs:
  version:
    description: Description
    required: false
    default: 1.2.3
runs:
  using: composite
  steps:
    - name: Print version
      shell: bash
      run: |
        echo 'version: ${{ inputs.version }}'
branding:
  icon: terminal
  color: gray-dark
